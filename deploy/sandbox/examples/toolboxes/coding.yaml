---
schema: ghostai.toolbox/1
name: coding
label: Repository inspection
notes: Read-only. Nothing here writes to the repository or reaches the network.
tools:
  - name: git_status
    definition: git-status
    permission: allow
  - name: git_diff
    definition: git-diff
    permission: allow
  - name: read_transcript
    definition: read-transcript
    permission: allow
